<!DOCTYPE html>

{% load static %}

<html>
<head>
    <link href="{% static 'css/bootstrap.min.css' %}" rel="stylesheet">
    <link href="{% static 'css/bootstrap-theme.min.css' %}" rel="stylesheet">
    <script src="{% static 'js/jquery-2.1.0.min.js' %}"></script>
    <script src="{% static 'js/bootstrap.min.js' %}"></script>

    <script>
        $(document).ready(function() {
            $("#send-notification-button").popover({
                container: "body",
                html: true,
                placement: "right",
                content: function() {
		    alert("!");
                    return $("#send-notification-popover").html();
                },
                trigger: "click"
            });
        });
    </script>

    <title>Articles</title>
</head>
<body>
    <div class="container-fluid">
        <div class="panel panel-default">
            <div class="panel-body">
                <div class="row">
                    <div class="col-md-2">
                        <div id="find-popover" class="hide">
                            <form id="find-form" role="form" action="{% url 'articles:index' %}" method="post">
                                {% csrf_token %}
                                <div class="form-group">
                                    <label for="verb"> <button type="submit" class="btn btn-default">Find</button></label>
                                    <input type="text" class="form-control" id="verb" name="find" placeholder="article">
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-12">
                    </div>
                </div>
            </div>
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>Venue</th>
                        <th>Title</th>
                        <th>Year</th>
                    </tr>
                </thead>
                <tbody>
                    {% if latest_articles_list %}
		    
    		    <ul>
    	    		{% for article in latest_articles_list %}
			<tr>
      			<th>{{ article.venue }}</th>
			<th><a href="/articles/{{ article.id }}">{{article.title}}</a></th>
			<th>{{ article.year }}</th>
			</tr>
    			{% endfor %}
    		    </ul>
 		    {% else %}
    			<p>No articles are available.</p>
		    {% endif %}
		    
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
